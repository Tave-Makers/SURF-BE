## [API 기능 이름]

### 개요

이 API의 주요 기능과 목적을 간략하게 설명합니다. (예: 새로운 사용자를 시스템에 등록합니다.)

### 📌 엔드포인트

`[메서드] [엔드포인트 URL]`

> 예시: POST /users
>

### 🔐 인증

- **인증 필요 여부:** (예: JWT 인증 필요, API Key 필요, 필요 없음)

> 인증이 필요한 경우 작성:
>
>
> 요청 헤더(Header)에 아래와 같이 Authorization 필드를 포함해야 합니다.
>
> `Authorization: Bearer {JWT_TOKEN}`
>

### ▶️ 요청 (Request)

<aside>

### Headers

| Key | Type | 설명 | 필수 |
| --- | --- | --- | --- |
| `Content-Type` | String | `application/json` | ✅ |
| `[헤더 파라미터명]` | String | [파라미터 설명] | (✅ / ⬜️) |

### Path Parameters

| Key | Type | 설명 |
| --- | --- | --- |
| `[경로 파라미터명]` | [타입] | [파라미터 설명] (예: 조회할 사용자의 고유 ID) |

### Query Parameters

| Key | Type | 설명 | 필수 |
| --- | --- | --- | --- |
| `[쿼리 파라미터명]` | [타입] | [파라미터 설명] (예: 한 페이지에 보여줄 아이템 개수) | (✅ / ⬜️) |

### Body

| Key | Type | 설명 | 필수 |
| --- | --- | --- | --- |
| `[요청 필드명]` | [타입] | [필드 설명] | (✅ / ⬜️) |
| `[요청 필드명]` | [타입] | [필드 설명] | (✅ / ⬜️) |

### ▶️요청 예시

```json

```

</aside>

---

---

### ◀️ 응답 (Response)

<aside>

### 성공 (Success)

| HTTP Status | 의미 |
| --- | --- |
| `[성공 코드]` | `[성공 코드 설명]` (예: 201 Created) |

**Body**

| Key | Type | 설명 |
| --- | --- | --- |
| `[응답 필드명]` | [타입] | [필드 설명] |
| `[응답 필드명]` | [타입] | [필드 설명] |

### 응답 예시

```json

```

</aside>

---

### 실패 (Error)

<aside>

| HTTP Status | 의미 | 에러 코드 |
| --- | --- | --- |
| `[에러 코드]` | `[에러 코드 설명]` (예: 400 Bad Request) | `[내부 에러 코드]` |
| `[에러 코드]` | `[에러 코드 설명]` (예: 404 Not Found) | `[내부 에러 코드]` |

**응답 예시 (400 Bad Request)**

```json
// 실패 응답 예시
{
  "status": 400,
  "errorCode": "INVALID_INPUT",
  "message": "입력값이 유효하지 않습니다."
}
```

</aside>